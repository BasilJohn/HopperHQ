import { StyleSheet } from "react-native";
import { primaryColor } from "../../common/colorConstants";
export const styles = StyleSheet.create({
  likeContainer: {
    height: 50,
    width: 50,
    borderRadius: 25,
    backgroundColor: primaryColor,
    justifyContent: "center",
    alignItems: "center",
  },
});
