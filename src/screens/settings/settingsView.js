import React from "react";
import { Text, View } from "react-native";
import { commonStyles } from "../../common/commonStyles";
const { pageStyle } = commonStyles;
const Settings = () => {
  return (
    <View style={pageStyle}>
      <Text>Settings</Text>
    </View>
  );
};

export default Settings;
